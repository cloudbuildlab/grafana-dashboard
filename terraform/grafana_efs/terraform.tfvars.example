ecs_cluster_arn    = "arn:aws:ecs:REGION:ACCOUNT:cluster/YOUR_CLUSTER"
vpc_id             = "vpc-xxxxx"
private_subnet_ids = ["subnet-xxx", "subnet-yyy", "subnet-zzz"]
public_subnet_ids  = ["subnet-aaa", "subnet-bbb"]
environment        = "dev"
desired_count      = 1

# Optional: CIDRs allowed to reach the ALB (if set, deployer IP is not used)
# alb_ingress_cidrs = ["10.0.0.0/8", "192.168.1.0/24"]

# Grafana (default: grafana/grafana:10.4.0 on port 3000)
# grafana_image = "grafana/grafana:10.4.0"
# grafana_port  = 3000

# Optional: set admin password. Stored in SSM Parameter Store and injected at runtime. Leave unset for default admin/admin.
# grafana_admin_password = "..."
